# E2E test fixture for the Burn-My-Windows shader animations. Used by
# tests/testcases/shader_animation.py via tests/run_one_test.sh.
animations = ({
    triggers = ["open"];
    opacity = 1;
    user-duration = 1.0;
    raw-progress = {
        curve = "linear";
        duration = "user-duration";
        start = 0;
        end = 1;
    };
    shader-progress = "raw-progress";
    shader-duration = "0 - user-duration";
    shader = "bmw-fire.frag";
}, {
    triggers = ["close"];
    opacity = 1;
    user-duration = 1.0;
    raw-progress = {
        curve = "linear";
        duration = "user-duration";
        start = 0;
        end = 1;
    };
    shader-progress = "raw-progress";
    shader-duration = "user-duration";
    shader = "bmw-fire.frag";
});
